   <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="{{ route('students.index') }}">QLSV</a>
            <div class="navbar-nav">
                <a class="nav-link" href="{{ route('students.index') }}">Danh sách sinh viên</a>
                <a class="nav-link" href="{{ route('students.create') }}">Thêm sinh viên</a>
            </div>
        </div>
    </nav>
